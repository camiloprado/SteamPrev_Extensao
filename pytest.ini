[pytest]
markers =
    unit: Testes unitários com mocks estritos. Não devem fazer chamadas de rede.
    integration: Testes de integração reais. Fazem chamadas à rede e precisam do .env real configurado.
testpaths =
    tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
